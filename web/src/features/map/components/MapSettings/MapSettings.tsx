import { Button, Stack } from '@mantine/core';

export default function MapSettings() {
  return (
    <Stack h="100%">
      <Button variant="filled" size="md" color="dark" fullWidth>
        Apply
      </Button>
    </Stack>
  );
}
